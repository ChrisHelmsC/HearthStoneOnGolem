import { Card } from "./cards/card";

export class Board {
    player1Cards : Array<Card>;   
    player2Cards : Array<Card>;

    constructor(p1Cards : Array<Card>, p2Cards : Array<Card>) {
        
    }
}